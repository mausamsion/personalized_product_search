model_name: pps_model
# model related
embedding_dim: 128
max_input_length: 128
num_layers: 2
num_heads: 4
# training related
batch_size: 64
lr: 0.0005
epochs: 50
log_step: 100
save_embs_epoch: 5
num_workers: 8
# others
device: torch.device('cuda' if torch.cuda.is_available() else 'cpu')
seed: 3367
